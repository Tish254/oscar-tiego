# Supabase Configuration
# Get these values from your Supabase project dashboard at https://app.supabase.com

# Public Supabase URL (safe to expose in browser)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Public anonymous/anon key (safe to expose in browser)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Service role key (NEVER expose in browser, server-side only)
# This key has elevated privileges and should only be used in server-side code
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# JWT secret for token verification (optional, for advanced auth validation)
SUPABASE_JWT_SECRET=your-jwt-secret-here
